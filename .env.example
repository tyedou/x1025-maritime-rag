# Hugging Face configuration
# Set HF_TOKEN if downloading gated models
HF_TOKEN=""

# Set HF_HOME to a persistent directory to cache downloaded models.
# Change this path to where you want models stored.
# HF_HOME=""  # uncomment and set to override; defaults to ~/hf_cache